const myFunction = (number) => {
  const functionArray = [
    (num) => (num += 1),
    (num) => (num *= 2),
    (num) => (num *= 534),
    (num) => (num /= 1),
    (num) => (math.round()),
    // add function here...
  ];

  for (const number of functionArray) {
      let number = 0
    console.log functionArray
  }
}